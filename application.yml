spring:
  profiles:
    active:
    - dev
---
spring:
  profiles: dev
    application:
      name: spring-cloud-config-dev #开发配置
---
spring:
  profiles: test
    application:
      name: spring-cloud-config-test